name: TEMP FASE42E help patch

on:
  push:
    branches:
      - feature/fase42e-badskisse-forbedringer

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/fase42e-badskisse-forbedringer
          fetch-depth: 0
      - name: Apply Help and QA patch
        run: python scripts/temp-fase42e-help-patch.py
      - name: Commit result and remove temporary files
        run: |
          rm scripts/temp-fase42e-help-patch.py .github/workflows/fase42e-help-patch.yml
          git config user.name "ExpoProffsenter"
          git config user.email "firmapost@ringside.no"
          git add src/modules/help/helpToolsCore.js scripts/critical-bathroom-sketch-check.mjs scripts/temp-fase42e-help-patch.py .github/workflows/fase42e-help-patch.yml
          git commit -m "FASE 42E: oppdater Hjelp og QA for Badskisse mål"
          git push origin HEAD:feature/fase42e-badskisse-forbedringer
